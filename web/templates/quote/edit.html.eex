<h1>edit quote</h1>

<%= form_for @changeset, quote_path(@conn, :update, @quote), fn f -> %>
  <h5>Body</h5>
  <div class="red"><%= error_tag f, :body %></div>
  <%= text_input f, :body, placeholder: "quote body", class: "w7" %>

  <%= submit "edit quote" %>
<% end %>
